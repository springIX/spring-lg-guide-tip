document.addEventListener("DOMContentLoaded", function () {
    LGE_CONFIGURATION_OPTION.initialize("Styler");

    // Styler 전용:
    const elStyler = document.querySelector(".test---Styler");
    if (elStyler) {
    }
});
